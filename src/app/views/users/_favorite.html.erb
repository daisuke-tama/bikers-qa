<!-- users/show内のお気に入り記事のいいね OFF機能(Ajax) -->
<!-- いいねをしてある => 記事＋お気に入り解除ボタンを表示 => 解除ボタンを押す =>  記事もボタンも削除(Ajax) -->
<!-- いいねをしてない => 何も表示しない -->
<% if article.favorited_by?(current_user) %>
  <p>
    <span>お気に入り解除: </span><%= link_to article_favorites_path(article.id), method: :delete, remote: true do %>
      <i class="fas fa-heart favorites-icon"></i>
    <% end %>
  </p>
<% end %>
